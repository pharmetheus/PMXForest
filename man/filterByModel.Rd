% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filterByModel.R
\name{filterByModel}
\alias{filterByModel}
\title{Apply a Model's $DATA IGNORE and ACCEPT Statements to a Data Frame}
\usage{
filterByModel(
  data,
  modFile,
  useInputNames = FALSE,
  quiet = FALSE,
  idVar = "ID",
  dropNoObs = FALSE
)
}
\arguments{
\item{data}{A data frame holding at least the first \verb{length($INPUT)} columns of
the model's data file, in that order.}

\item{modFile}{Path to the NONMEM control stream.}

\item{useInputNames}{Logical. If \code{FALSE} (default), the filtered rows are
returned with the caller's own column names. If \code{TRUE}, the result is the
data as NONMEM sees it: the first \verb{length($INPUT)} columns only, renamed to
the \verb{$INPUT} names.}

\item{quiet}{Logical. If \code{FALSE} (default), reports the condition applied and
how many records and subjects it removed.}

\item{idVar}{The subject identifier, used only for that report. Defaults to
\code{"ID"}.}

\item{dropNoObs}{Logical. If \code{TRUE}, also remove subjects left with no
observation record after filtering - subjects with only dose or other
event records. Defaults to \code{FALSE}, which keeps every subject NONMEM
counts. See Details.}
}
\value{
A data frame containing the rows the model used.
}
\description{
Reads the \code{IGNORE} and \code{ACCEPT} statements from a NONMEM control
stream's \verb{$DATA} record and applies them to a data frame, returning the rows
the model actually used. Summarising the raw analysis file instead of the
filtered one gives covariate quantiles and reference values for a population
the model never saw.
}
\details{
\strong{Columns are matched by position, not by name.} NONMEM skips the header
line and takes its column names from \verb{$INPUT}, so the names in the data file
need not agree with the names the model uses - and where they disagree,
filtering by name reads the wrong column. This function maps the first
\verb{length($INPUT)} columns of \code{data} onto the \verb{$INPUT} names before evaluating
any condition. Columns beyond that are ignored, as NONMEM ignores them, and
\code{DROP}/\code{SKIP} columns still occupy a position. A \code{SYNONYM=REAL} pair can be
referred to by either name.

\strong{What is supported.} \code{IGNORE=(list)}, \code{IGNORE(list)}, \code{ACCEPT=(list)} and
\code{ACCEPT(list)}, with the \code{.EQ.}/\code{.NE.}/\code{.GT.}/\code{.GE.}/\code{.LT.}/\code{.LE.} operator
family, their \code{.EQN.}/\code{.NEN.} variants, and a bare \code{=} meaning equality.
Conditions separated by commas are combined with OR, as are multiple
statements. A record is dropped if it matches any \code{IGNORE} condition, and
kept only if it matches some \code{ACCEPT} condition. NONMEM does not allow both
forms in one \verb{$DATA} record and neither does this function.

\strong{Two comparison families, as in NM-TRAN.} \code{=}, \code{==}, \verb{/=}, \code{.EQ.} and
\code{.NE.} compare the value as \strong{text}; \code{.EQN.}, \code{.NEN.} and the inequalities
compare it \strong{numerically}. This is NONMEM's own rule and it matters: a
control stream filtering a NONMEM table file, where an integer \code{1} is
written \code{1.0000E+00}, gets nothing from \code{IGNORE=(OCC.EQ.1)} and everything
it expects from \code{IGNORE=(OCC.EQN.1)}. That is why \code{.EQN.}/\code{.NEN.} exist,
and why \code{run7.mod} uses them.

There is a limit to how faithfully the text comparison can be reproduced
here. NONMEM compares against the text in the \strong{data file}; this function
is handed a data frame, in which that text has already been parsed into
numbers, so it compares against \code{as.character()} of the value instead. The
two agree whenever the file wrote the value the way R renders it, which is
the ordinary case for a CSV of integers. They can disagree for a file whose
numbers carry a format of their own - a table file again. When a text
comparison selects no record but the numeric reading of the same condition
would have selected some, that is the signature of this disagreement and
the function warns, naming both texts. Prefer \code{.EQN.}/\code{.NEN.} in a
condition whose result matters.

\strong{What is not.} A value compared as text (\code{IGNORE=(GEN='M')}) is refused:
NONMEM permits it, but the condition grammar shared with \verb{$PK} has no string
literal. The single-character form (\verb{IGNORE=@}, \code{IGNORE=C}) is a rule
about the first non-blank character of the raw record rather than a condition
on the data, so it cannot be applied to a data frame. \verb{IGNORE=@} is the usual
way of skipping a header line, which \code{read.csv()} has already done, so this
is normally harmless - a warning is issued and the statement is skipped.

\strong{Subjects without observations.} NONMEM reads a subject whose records
are all doses or other events, and counts it among its individuals, but
such a subject contributes nothing to the estimates. \code{dropNoObs = TRUE}
removes them after the \verb{$DATA} filter, so they do not enter covariate
summaries either. An observation is identified as NONMEM identifies it,
from the columns \verb{$INPUT} declares and does not drop:
\itemize{
\item \code{MDV == 0} where there is an \code{MDV} column. \code{MDV = 100} is ignored
during estimation and does not count.
\item Otherwise, in a \verb{$PRED} model every record is an observation, since
\code{EVID} and the dose items belong to PREDPP.
\item Otherwise \code{EVID == 0} where there is an \code{EVID} column.
\item Otherwise a record is a dose if any of \code{AMT}, \code{RATE} or \code{SS} is
non-zero - the rule NM-TRAN uses to supply \code{EVID} - and an observation
if not. A steady-state infusion has \code{AMT = 0}, which is why \code{AMT} alone
is not enough.
}
A subject is a run of consecutive records with the same \code{ID}, as NONMEM
reads it. In a FREM data set covariates are observation records, so there a
subject with covariates but no other observations is kept.
}
\examples{
modFile <- system.file("extdata", "SimVal/run7.mod", package = "PMXForest")
dfData <- read.csv(
  system.file("extdata", "SimVal/DAT-1-MI-PMX-2.csv", package = "PMXForest")
)

## run7.mod drops BLQ records, TYPE 2 records and one subject entirely
dfUsed <- filterByModel(dfData, modFile)
c(raw = length(unique(dfData$ID)), used = length(unique(dfUsed$ID)))

## Covariate quantiles differ between the two populations
getCovStats(dfData, "CRCL", idVar = "ID")
getCovStats(dfUsed, "CRCL", idVar = "ID")

## 32 of the subjects run7 reads have dose records only
dfObs <- filterByModel(dfData, modFile, dropNoObs = TRUE)
length(unique(dfObs$ID))

## The names $INPUT uses are not the names in this csv file
head(names(filterByModel(dfData, modFile, useInputNames = TRUE, quiet = TRUE)), 10)
}
\seealso{
\code{\link[=verifyFilterByModel]{verifyFilterByModel()}}, which checks the result against the
record, subject and observation counts NONMEM printed in the \code{.lst}.
\code{\link[=createParamFunction]{createParamFunction()}}, \code{\link[=setupDfCovs]{setupDfCovs()}}, \code{\link[=getCovStats]{getCovStats()}}
\code{vignette("Part2-walkthrough", package = "PMXForest")} for how this fits the whole workflow.
}
