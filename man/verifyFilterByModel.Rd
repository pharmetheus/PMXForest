% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verifyFilterByModel.R
\name{verifyFilterByModel}
\alias{verifyFilterByModel}
\title{Check \code{filterByModel()} against NONMEM's own account of the data}
\usage{
verifyFilterByModel(
  modFile,
  data = NULL,
  lstFile = NULL,
  idVar = "ID",
  quiet = FALSE
)
}
\arguments{
\item{modFile}{Path to the control stream.}

\item{data}{The data set as a data frame. Optional: when absent the \verb{$DATA}
record is resolved relative to \code{modFile} and read.}

\item{lstFile}{Path to the run's \code{.lst}. Optional: \verb{<model>.lst}, \code{.res},
\code{.out} and \code{NM_run1/psn.lst} are tried in turn.}

\item{idVar}{Name of the subject column, for the subject count.}

\item{quiet}{Suppress the progress messages from \code{\link[=filterByModel]{filterByModel()}}.}
}
\value{
A length-1 logical, usable directly in an \code{if}, with attributes:
\itemize{
\item \code{checks} - a data frame of \code{CHECK}, \code{FILTERED}, \code{NONMEM}, \code{DIFF}
and \code{PASS}, one row per comparison.
\item \code{informative} - \code{FALSE} when the \verb{$DATA} record removed nothing, so
the comparison could not have failed.
\item \code{removed} - how many records the \verb{$DATA} record removed, measured
against NONMEM's own count (\code{rawRows} minus \verb{NO. OF DATA RECS}) and so
independent of what \code{\link[=filterByModel]{filterByModel()}} did. On a failing check it will
not reconcile with the filtered count, which is the point.
\item \code{rawRows} - rows in the data set as read, before filtering.
\item \code{obsBasis} - the column(s) observations were identified from:
\code{"MDV"}, \code{"EVID"}, the dose items present (e.g. \code{"AMT/RATE"}), or
\code{"NONE"} when every record is an observation.
}
}
\description{
Every NONMEM run prints how many records, subjects and
observations it read once the \verb{$DATA} record had been applied. Those three
numbers are an independent check on \code{\link[=filterByModel]{filterByModel()}}: they come from
NONMEM, not from a reading of the control stream, so they catch a filter
that is wrong in the same way your own reading of \verb{$DATA} is wrong.

It needs nothing but artefacts already on disk - the control stream, its
\code{.lst}, and the data file - and never re-runs anything.
}
\details{
The observation count follows NONMEM, using the rule described
under \strong{Subjects without observations} in \code{\link[=filterByModel]{filterByModel()}}: \code{MDV == 0}
where there is an \code{MDV} column, otherwise \code{EVID == 0}, otherwise records
with no non-zero \code{AMT}, \code{RATE} or \code{SS}. Columns are read by position under
the \verb{$INPUT} names.

The \code{.lst} describes the model NONMEM read, which is not always the model
on disk. Where its echoed \verb{$INPUT} or \verb{$DATA} filter clauses disagree with
\code{modFile}, the counts no longer describe it and the comparison is noise, so
this warns. Columns are matched by position, so a changed \verb{$INPUT} matters
even when the names are all still there.

A \verb{$DATA} record that removes nothing agrees with the \code{.lst} whatever
\code{\link[=filterByModel]{filterByModel()}} does. That is reported as \code{attr(., "informative")} rather
than hidden, because such a result is not evidence of anything.
}
\examples{
modFile <- system.file("extdata", "SimVal/run7.mod", package = "PMXForest")
v <- verifyFilterByModel(modFile, quiet = TRUE)
v
if (v) message("filterByModel() reproduced the run")
attr(v, "checks")
}
\seealso{
\code{\link[=filterByModel]{filterByModel()}} for the filtering itself, and
\code{\link[=verifyParamFunction]{verifyParamFunction()}}, which does the same job for a generated parameter
function.
}
