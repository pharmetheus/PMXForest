% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotForestDF.R
\name{plotForestDF}
\alias{plotForestDF}
\title{plotForestDF()}
\usage{
plotForestDF(df, plotRelative = TRUE, addTable = TRUE,
  strxlab = "Covariate-Parameter effect", strylab = "", decdig = 2,
  percentscalexmax = 1.5, xtextoffset = 0.25,
  xtextoffsetpercent = 1.1, textsize = 5, vlinecol = "lightgrey",
  vlinesize = 1.5, pointsize = 3, errorbarsize = 1,
  freescalex_wrap = (!plotRelative || addTable), log10x = FALSE,
  fixedSpacing = TRUE, groupdist = 0.3, withingroupdist = 0.2)
}
\arguments{
\item{df}{the forest plot data frame, should be based on a call to getForestDF or manually created accfording to the getForestDF structure}

\item{plotRelative}{if the plot should be based on the relative scale to the reference (or parameter scale), default = TRUE}

\item{addTable}{if a CI table should be added to the plot, default = TRUE}

\item{strxlab}{x-label, default = "Covariate-Parameter effect"}

\item{strylab}{y-label, default = ""}

\item{decdig}{number of digits after the comma in the CI table, only used with addTable = TRUE, default=2}

\item{percentscalexmax}{increase of xmax, default 1.5 (fraction of x-axis), only used with addTable = TRUE}

\item{xtextoffset}{textoffset, default 0.25 on x-axis scale, only used with addTable = TRUE}

\item{xtextoffsetpercent}{default = 1.1, scale the xaxis by the fraction, only used if addTable = TRUE}

\item{textsize}{default = 6, text size of the CI text, only used if addTable = TRUE}

\item{vlinecol}{color of vertical reference line, default "lightgrey"}

\item{vlinesize}{size of vertical reference line, default 1.5}

\item{pointsize}{size of geom point (e.g. median value), default 3}

\item{errorbarsize}{size of errorbars, default 1}

\item{freescalex_wrap}{if wrap as free_x or not, default based on plotRelative and addTable}

\item{log10x}{if the x-axis should be set to a log10 axis, default=FALSE}

\item{fixedSpacing}{A boolean (TRUE/FALSE) if fixed spacing between covariate groups should be used in the Forest plot. Default is TRUE.
If FALSE, the y coordinates are calculated relative to the number of groups and numbers of covariates within a group.
If fixed spacing is used, groupdist and withingroupdist will be used as well.}

\item{groupeddist}{A number defining the y distance between groups of covariates.}

\item{withingroupeddist}{A number defining the y distance within groups of covariates.}
}
\value{
a ggplot2 object with a Forest plot
}
\description{
plotForestDF()
}
\examples{
#' \dontrun{
plotForestDF(dfresCOV,textsize = 5) +
xlim(0,3.3) +
geom_vline(xintercept=c(0.8,1.25),linetype="dashed")
}
}
