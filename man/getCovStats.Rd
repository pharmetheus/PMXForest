% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getCovStats.R
\name{getCovStats}
\alias{getCovStats}
\title{getCovStats}
\usage{
getCovStats(
  data,
  covariates,
  minLevels = 10,
  probs = c(0.05, 0.95),
  idVar = "ID",
  missVal = -99,
  nsig = 3
)
}
\arguments{
\item{data}{A data frame that includes the covariates to summarise. Only the first line per subject will be used in the summary.}

\item{covariates}{A character vector of covariate names as they appear in the data file.}

\item{minLevels}{The maximum number of unique values for a covariate to be regarded as continus. Devfault is 10.}

\item{probs}{A vector of two number - the lower and upper percentiles to use for summarising continuous covariates.}

\item{idVar}{The name of the ID column to use for identifying one record per subject.}

\item{missVal}{The missing value indicator. Default is -99.}

\item{nsig}{The number of `digits` passed to the `signif` function when applied
to covariates with more than `minLevels` of unique values.}
}
\value{
A data frame with covariate statistics to be used as an argument for Forest plots.
}
\description{
To quickly get a dfCovs data from for Forest plots by just supplying a data file and a vector of covariate names.
}
\examples{
\dontrun{

data <- read.csv("inst/extdata/DAT-1-MI-PMX-2.csv")

dfCovs <- createInputForestData(
  getCovStats(data,c("WT","BMI","SEX"),missVal=-99),
  iMiss    =-99
)}

}
